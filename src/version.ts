// Single source of truth for the published version. Used in the MCP handshake
// (so a probed server can log which routeproof connected) and by `--version`,
// so the two can never drift apart.
export const VERSION = "0.3.2";
